# 📋 Equipment Request System - 면접 질문 리스트 (100문항)

> 이 프로젝트를 기반으로 한 기술 면접 대비 질문입니다. 난이도: ★ 쉬움 | ★★ 보통 | ★★★ 어려움

---

## 1. JPA / 도메인 설계 (15문항)

### ★ 쉬움

**Q1.** `@Entity`와 `@Table` 어노테이션의 역할은 무엇인가요? 

User.java에서 `@Table(name = "users")`로 지정한 이유는?

> 💡 `user`는 SQL 예약어이므로 `users`로 지정

**Q2.** `@Id`와 `@GeneratedValue(strategy = GenerationType.IDENTITY)`의 차이를 설명하세요. 

BaseEntity.java에서 왜 `IDENTITY` 전략을 사용했나요?

> 💡 MySQL/MariaDB의 AUTO_INCREMENT와 매핑, SEQUENCE vs TABLE vs IDENTITY 비교

**Q3.** `@Column(nullable = false)`, `@Column(unique = true)`, `@Column(columnDefinition = "TEXT")`의 차이를 설명하세요.

> 💡 DDL 자동 생성 시 NOT NULL, UNIQUE 제약조건, 컬럼 타입 지정

**Q4.** `@Enumerated(EnumType.STRING)`을 사용한 이유는? `EnumType.ORDINAL`과의 차이점은?

> 💡 `User.UserRole`과 `RequestStatus`에서 사용. ORDINAL은 순서 변경 시 데이터 오염 위험

**Q5.** 

BaseEntity.java를 `abstract class`로 선언한 이유는? `@MappedSuperclass`의 역할은?

> 💡 단독 테이블 생성 불필요, 상속받는 엔티티에만 매핑 정보 제공

### ★★ 보통

**Q6.** 

EquipmentRequest.java에서 `@ManyToOne(fetch = FetchType.LAZY)`와 `FetchType.EAGER`의 차이를 설명하세요. 왜 LAZY를 기본으로 사용하나요?

> 💡 성능 최적화, N+1 문제와의 관련성, 프록시 객체 동작 원리

**Q7.** 

EquipmentRequest와 

RequestItem의 양방향 연관관계에서 

addItem() 편의 메서드가 왜 필요한가요?

java

public void addItem(RequestItem item) {

    this.items.add(item);

    item.setEquipmentRequest(this); // 왜 이 줄이 필요한가?

}

> 💡 양방향 관계에서 객체 그래프 탐색의 일관성 보장, 영속성 컨텍스트에서의 동작

**Q8.** `@OneToMany(mappedBy = "equipmentRequest", cascade = CascadeType.ALL)`에서 `mappedBy`와 `cascade`의 역할은?

> 💡 연관관계의 주인 개념, CASCADE 종류(PERSIST, REMOVE, ALL 등)

**Q9.** `@PrePersist`를 사용하여 `createdAt`을 자동 설정하는 방식의 장단점은? `@CreatedDate`와 비교하면?

> 💡 JPA 콜백 vs Spring Data Auditing, `@EnableJpaAuditing` 설정 여부

**Q10.** `@Builder.Default`가 `version = 0L`과 `items = new ArrayList<>()`에 사용된 이유는?

> 💡 Lombok Builder 패턴에서 기본값이 무시되는 문제 해결

### ★★★ 어려움

**Q11.** 

Equipment.java의 `@Version` 어노테이션이 하는 역할은? 낙관적 잠금(Optimistic Lock)과 비관적 잠금(Pessimistic Lock)의 차이를 설명하세요.

> 💡 동시 재고 감소 시 발생할 수 있는 Lost Update 문제, OptimisticLockException 처리

**Q12.** 

decreaseStock() 메서드가 도메인 엔티티 안에 있는 이유는? 서비스 레이어에 두면 어떤 문제가 생기나요?

> 💡 DDD의 Rich Domain Model vs Anemic Domain Model, 도메인 로직의 응집도

**Q13.** 

EquipmentRequest에서 `@OneToMany(fetch = FetchType.LAZY)`로 변경했다는 주석이 있습니다. EAGER에서 LAZY로 변경했을 때 어떤 문제가 발생할 수 있고, 어떻게 해결했나요?

> 💡 LazyInitializationException, `@Transactional` 범위, Fetch Join으로 해결

**Q14.** 

RequestItem이 

Equipment와 

EquipmentRequest 양쪽에 `@ManyToOne`으로 연결되어 있습니다. 이런 **중간 테이블 엔티티** 패턴의 장점과 `@ManyToMany` 대비 장점은?

> 💡 추가 속성(quantity) 관리, `@ManyToMany`의 한계, 조인 테이블 직접 제어

**Q15.** `@NoArgsConstructor(access = AccessLevel.PROTECTED)`의 의미는? 왜 `PROTECTED`로 설정하나요?

> 💡 JPA 프록시 생성을 위한 기본 생성자 필요, 외부에서의 무분별한 객체 생성 방지

---

## 2. Spring Security / JWT 인증 (12문항)

### ★ 쉬움

**Q16.** JWT(JSON Web Token)란 무엇이고, 세션 기반 인증과의 차이점은?

> 💡 Stateless vs Stateful, 토큰 구조(Header.Payload.Signature)

**Q17.** 

SecurityConfig.java에서 `csrf.disable()`을 한 이유는?

> 💡 REST API는 CSRF 토큰 불필요 (브라우저 쿠키 기반이 아닌 Authorization 헤더), SPA 환경

**Q18.** `SessionCreationPolicy.STATELESS`의 의미는?

> 💡 서버가 세션을 생성하지 않음, JWT 토큰으로 매 요청마다 인증

**Q19.** `BCryptPasswordEncoder`는 왜 사용하나요? 단순 해시(MD5, SHA)와의 차이는?

> 💡 salt 자동 생성, 작업 비용(work factor) 조절 가능, Rainbow Table 공격 방지

### ★★ 보통

**Q20.** 

JwtAuthenticationFilter.java가 `OncePerRequestFilter`를 상속하는 이유는?

> 💡 요청당 한 번만 실행 보장, forward/redirect 시 중복 실행 방지

**Q21.** JWT 필터에서 `UsernamePasswordAuthenticationToken`의 principal에 `userId`를 넣은 이유는? `email`을 넣으면 안 되나요?

> 💡 `Authentication.getName()`으로 쉽게 추출, Controller에서의 활용 패턴

**Q22.** `SecurityFilterChain`에서 `.hasRole("ADMIN")`은 내부적으로 어떻게 동작하나요? 필터에서 `"ROLE_" + role`로 prefix를 붙이는 이유는?

> 💡 `hasRole()`은 자동으로 `ROLE_` prefix 추가, `hasAuthority()`와의 차이

**Q23.** Access Token과 Refresh Token을 분리하는 이유는? 현재 프로젝트에서 Refresh Token이 실제로 사용되고 있나요?

> 💡 보안과 사용성의 균형, Access Token 짧은 만료 + Refresh Token으로 재발급, 현재는 발급만 하고 갱신 로직 미구현

**Q24.** 

JwtUtil.java에서 `Keys.hmacShaKeyFor(secret.getBytes())`의 의미는? HS256 알고리즘의 동작 원리는?

> 💡 HMAC-SHA256 대칭키 알고리즘, 비밀키 길이 요구사항(최소 256비트)

### ★★★ 어려움

**Q25.** 

validateToken()과 

isTokenExpired() 두 메서드가 모두 호출되는 이유는? 

getClaims()에서 만료된 토큰도 예외가 발생하지 않나요?

> 💡 jjwt 라이브러리 버전에 따른 동작 차이, 방어적 프로그래밍

**Q26.** 현재 JWT 인증 구조의 보안 취약점을 3가지 이상 지적하고 개선 방안을 제안하세요.

> 💡 토큰 탈취 시 무효화 불가(Token Blacklist), Refresh Token 저장 위치(localStorage vs httpOnly Cookie), 토큰 갱신 로직 미구현, XSS 취약성

**Q27.** Spring Security Filter Chain에서 JWT 필터가 `UsernamePasswordAuthenticationFilter` **앞에** 추가되는 이유는?

> 💡 Filter Chain 순서와 동작 원리, 인증 객체가 SecurityContext에 먼저 설정되어야 하는 이유

---

## 3. Spring Boot 아키텍처 (12문항)

### ★ 쉬움

**Q28.** `@RestController`와 `@Controller`의 차이는?

> 💡 `@ResponseBody` 자동 적용 여부, JSON 응답 반환

**Q29.** `@Service`, `@Repository`, `@Component`의 차이를 설명하세요. 기능적 차이가 있나요?

> 💡 스테레오타입 어노테이션, 컴포넌트 스캔, `@Repository`의 예외 변환 기능

**Q30.** `@RequiredArgsConstructor`로 의존성을 주입하는 방식은? `@Autowired`와 비교하면?

> 💡 생성자 주입 vs 필드 주입, 불변성, 테스트 용이성, 순환 참조 감지

**Q31.** `@Transactional(readOnly = true)`를 클래스 레벨에 선언하고, CUD 메서드에 `@Transactional`을 따로 선언한 이유는?

> 💡 읽기 전용 최적화(Dirty Checking 비활성화, 플러시 생략), 메서드 레벨 우선 적용

### ★★ 보통

**Q32.** 

DataLoader.java가 `CommandLineRunner`를 구현한 이유는? 초기 데이터 로딩의 다른 방법은?

> 💡 `ApplicationRunner`, `data.sql`, `@PostConstruct`, Flyway/Liquibase 비교

**Q33.** 

DataLoader에서 `userRepository.count() > 0`일 때 return하는 이유는?

> 💡 멱등성(Idempotency), 애플리케이션 재시작 시 중복 데이터 방지

**Q34.** `@ModelAttribute`와 `@RequestBody`의 차이는? 

EquipmentController.java의 

create()에서 왜 `@ModelAttribute`를 사용했나요?

> 💡 `multipart/form-data` vs `application/json`, 파일 업로드 시 `@RequestBody` 사용 불가

**Q35.** 컨트롤러에서 내부 static class로 DTO를 정의한 방식의 장단점은? 별도 `dto` 패키지를 만드는 것과 비교하면?

> 💡 응집도 vs 재사용성, DTO의 범위와 책임

**Q36.** `@Value("${CORS_ALLOWED_ORIGINS:http://localhost:5173}")`에서 콜론(`:`)의 의미는?

> 💡 SpEL의 기본값 설정 문법, 환경 변수 미설정 시 폴백

### ★★★ 어려움

**Q37.** 

TimeTraceAop.java의 `@Around` 어드바이스가 동작하는 원리를 설명하세요. AOP 프록시는 어떻게 생성되나요?

> 💡 JDK Dynamic Proxy vs CGLIB Proxy, 포인트컷 표현식 `execution(* backend.controller..*(..))` 해석

**Q38.** `@Transactional`이 AOP 기반이라면, 같은 클래스 내에서 `@Transactional` 메서드를 호출하면 어떻게 되나요?

> 💡 Self-invocation 문제, 프록시 우회 이슈, 해결 방법

**Q39.** 현재 프로젝트에 **전역 예외 처리**가 없습니다(`TODO: 에러 전역 처리`). `@ControllerAdvice`와 `@ExceptionHandler`를 사용하여 어떻게 구현할 수 있나요?

> 💡 예외 계층 구조 설계, 커스텀 에러 응답 DTO, HTTP 상태 코드 매핑

---

## 4. REST API 설계 (8문항)

### ★ 쉬움

**Q40.** RESTful API에서 HTTP 메서드(GET, POST, PUT, DELETE)의 용도를 이 프로젝트 API를 예시로 설명하세요.

> 💡 `GET /api/equipment`, `POST /api/equipment`, `PUT /api/equipment/{id}`, `DELETE /api/equipment/{id}`

**Q41.** `@PathVariable`과 `@RequestParam`의 차이를 프로젝트 코드에서 찾아 설명하세요.

> 💡 `/equipment/{id}` vs `/equipment/search?keyword=...`

### ★★ 보통

**Q42.** 현재 API에서 에러 응답 형식이 통일되어 있지 않습니다. 어떤 문제가 있고 어떻게 개선할 수 있나요?

> 💡 
> 
> approve()는 String, 
> 
> create()는 객체 반환. 통일된 응답 형식(ApiResponse) 설계

**Q43.** `EquipmentResponse.from(Equipment equipment)` 같은 정적 팩토리 메서드 패턴의 장점은?

> 💡 Entity → DTO 변환의 명시성, 생성자 대비 가독성, 네이밍의 자유도

**Q44.** 현재 비품 목록 API가 **페이징을 지원하지 않습니다**. Spring Data의 `Pageable`을 이용하여 어떻게 구현하나요?

> 💡 `Page<Equipment>`, 
> 
> PageRequest, 프론트엔드의 
> 
> EquipmentListResponse 타입이 이미 페이징 구조를 갖고 있는 점

### ★★★ 어려움

**Q45.** `EquipmentRequestController.create()`에서 `Authentication authentication`을 파라미터로 받아 userId를 추출합니다. `@AuthenticationPrincipal`을 사용하는 방법과 비교하면?

> 💡 커스텀 `UserDetails` 구현, `@AuthenticationPrincipal` 리졸버, 현재 userId를 principal에 직접 넣은 구조의 한계

**Q46.** REST API의 HATEOAS 원칙이란 무엇이고, 현재 프로젝트에 적용한다면 어떤 변화가 필요한가요?

> 💡 `_links` 필드, Spring HATEOAS, 실무에서의 채택 여부

**Q47.** API 버전 관리(Versioning) 전략에는 어떤 것들이 있고, 이 프로젝트에 적용한다면?

> 💡 URI 버전 (`/api/v1/`), Header 버전, Query Parameter 버전

---

## 5. 프론트엔드 React / TypeScript (15문항)

### ★ 쉬움

**Q48.** React의 `useState`와 `useEffect`의 역할을 설명하세요. 

ImageUpload.tsx에서 `useState`가 어떻게 사용되었나요?

> 💡 `preview`, `fileName` 상태 관리, 리렌더링 트리거

**Q49.** TypeScript의 `interface`와 `type`의 차이는? 

types/index.ts에서 왜 `interface`를 사용했나요?

> 💡 선언 병합(Declaration Merging), 확장성, 컨벤션

**Q50.** React Router의 `<Navigate to="/login" replace />`에서 `replace`의 의미는?

> 💡 브라우저 히스토리 스택에서 현재 항목 대체, 뒤로가기 시 동작 차이

**Q51.** `React.ChangeEvent<HTMLInputElement>` 같은 이벤트 타입을 명시하는 이유는?

> 💡 TypeScript의 타입 안전성, `e.target.files`의 자동완성과 타입 체크

### ★★ 보통

**Q52.** 

router/index.tsx에서 `lazy()`와 `<Suspense>`를 사용한 이유와 동작 원리는?

> 💡 코드 스플리팅(Code Splitting), Dynamic Import, 번들 크기 최적화, 초기 로딩 속도

**Q53.** 

ProtectedRoute.tsx의 `adminOnly` prop 패턴을 설명하세요. 이것은 HOC인가요, 래퍼 컴포넌트인가요?

> 💡 Wrapper/Guard 패턴, `children` prop 활용, HOC vs Render Props vs 컴포넌트 합성

**Q54.** 

ImageUpload.tsx에서 `FileReader.readAsDataURL()`을 사용한 이유는? `URL.createObjectURL()`과의 차이는?

> 💡 Base64 인코딩 vs Blob URL, 메모리 관리(revoke 필요성), 미리보기 용도

**Q55.** `import.meta.env.VITE_API_URL`의 동작 원리는? `process.env`와의 차이는?

> 💡 Vite의 환경변수 처리, `VITE_` prefix 필수, 빌드 타임 치환

**Q56.** TypeScript에서 `User | null` 유니온 타입과 `user?.role` 옵셔널 체이닝의 관계를 설명하세요.

> 💡 null 안전성, 런타임 에러 방지, `strictNullChecks` 설정

### ★★★ 어려움

**Q57.** React의 리렌더링이 발생하는 조건 3가지를 나열하고, `cartStore`의 불변성 유지 패턴(`[...state.items]`)이 왜 필요한지 설명하세요.

> 💡 state 변경, props 변경, 부모 리렌더, 참조 비교(===), Immutability

**Q58.** `<Suspense fallback={<Loading />}>`에서 Error Boundary와 함께 사용하면 어떤 이점이 있나요? 현재 프로젝트에 Error Boundary가 구현되어 있나요?

> 💡 선언적 에러/로딩 처리, `componentDidCatch`, React 18의 동시성 모드와의 관계

**Q59.** React 19의 주요 변경사항은 무엇이고, 이 프로젝트에서 활용할 수 있는 새로운 기능은?

> 💡 use() Hook, Server Components, Actions, useOptimistic

**Q60.** `React.ReactNode`와 `React.ReactElement`의 차이는? 

ProtectedRoute에서 `children: React.ReactNode`를 사용한 이유는?

> 💡 `ReactNode`는 string/number/null 등도 포함, `ReactElement`은 JSX 객체만

**Q61.** React Router v7의 `createBrowserRouter`와 기존 `<BrowserRouter>` + `<Routes>` 방식의 차이는?

> 💡 Data Router, loader/action 패턴, 중첩 라우팅의 데이터 흐름

**Q62.** `Suspense`를 사용한 lazy loading이 SEO에 미치는 영향은? SSR과 CSR의 차이와 관련지어 설명하세요.

> 💡 CSR의 SEO 한계, Next.js/Remix의 SSR 접근, 이 프로젝트가 SPA인 이유(내부 시스템)

---

## 6. 상태 관리 (Zustand / TanStack Query) (10문항)

### ★ 쉬움

**Q63.** Zustand란 무엇이고, Redux와 비교했을 때 장점은?

> 💡 보일러플레이트 최소화, Provider 불필요, 번들 크기, 학습 곡선

**Q64.** 

authStore.ts에서 `persist` 미들웨어의 역할은 무엇인가요?

> 💡 `localStorage`에 상태 영속화, 새로고침 시 로그인 유지, `name: "auth-storage"` 키

### ★★ 보통

**Q65.** `useAuthStore`의 

setAuth에서 `useCartStore.getState().clearCart()`를 호출합니다. 이것이 **스토어 간 통신** 패턴인데, 장단점은?

> 💡 `getState()`는 리렌더링 없이 값 접근, 스토어 의존성/결합도, 이벤트 기반 대안

**Q66.** 

cartStore.ts의 

addItem에서 이미 있는 아이템의 수량을 증가시키는 로직을 설명하세요. **불변성**을 유지하고 있나요?

> 💡 `[...state.items]`로 새 배열 생성하지만, `updatedItems[existingItemIndex].quantity += newItem.quantity`는 직접 변경. Zustand의 Immer 미들웨어 대안

**Q67.** 

api.ts의 Axios Interceptor에서 `localStorage`를 직접 읽는 이유는? Zustand store에서 직접 가져올 수 없나요?

> 💡 모듈 간 순환 참조 방지, Axios 인스턴스의 생성 시점과 스토어 초기화 시점의 차이

**Q68.** 401 응답 시 `window.location.href = "/login"`으로 리다이렉트합니다. React Router의 `navigate()`를 사용하지 않은 이유와 차이점은?

> 💡 Axios interceptor는 React 컴포넌트 외부, React hooks 사용 불가, 전체 새로고침 발생

### ★★★ 어려움

**Q69.** 클라이언트 상태(Zustand)와 서버 상태(TanStack Query)를 분리하는 이유를 설명하세요. 어떤 데이터를 어디에 저장해야 하나요?

> 💡 authStore/cartStore = 클라이언트 상태, 비품 목록/신청 내역 = 서버 상태. 캐싱, stale-while-revalidate 전략

**Q70.** `persist`로 `localStorage`에 JWT 토큰을 저장하는 것의 보안 문제는? XSS 공격 시 어떻게 토큰이 탈취될 수 있나요?

> 💡 `httpOnly Cookie` 대안, Content-Security-Policy, DOMPurify

**Q71.** TanStack Query의 `useQuery`와 `useMutation`의 차이를 설명하고, 이 프로젝트의 어떤 API 호출에 각각 사용하면 적절한지 설명하세요.

> 💡 GET = useQuery, POST/PUT/DELETE = useMutation, 캐시 무효화(invalidateQueries)

**Q72.** Zustand의 

create 함수에 제네릭 `create<AuthState>()(persist(...))`에서 

()가 두 번 호출되는 이유는?

> 💡 TypeScript에서 curried function 패턴, 미들웨어 타입 추론 문제 해결

---

## 7. AWS 인프라 / 클라우드 (10문항)

### ★ 쉬움

**Q73.** AWS S3의 용도는 이 프로젝트에서 2가지입니다. 각각 설명하세요.

> 💡 ① 비품 이미지 저장, ② 프론트엔드 정적 파일 호스팅

**Q74.** CloudFront CDN을 사용하는 이유는?

> 💡 글로벌 엣지 로케이션, 캐싱으로 S3 부하 감소, HTTPS 지원, SPA 라우팅 처리

**Q75.** 

S3Config.java에서 `StaticCredentialsProvider`를 사용하는 방식의 문제점은?

> 💡 Access Key 직접 사용 → IAM Role 기반 인증(Instance Profile) 대안

### ★★ 보통

**Q76.** 

S3Service.java에서 파일명을 `UUID.randomUUID() + extension`으로 생성하는 이유는?

> 💡 파일명 충돌 방지, URL-safe 파일명, 기존 파일 덮어쓰기 방지

**Q77.** S3에 이미지를 업로드할 때 `contentType`을 설정하는 이유는?

> 💡 브라우저가 파일을 올바르게 렌더링하기 위함, 미설정 시 `application/octet-stream`으로 다운로드됨

**Q78.** `EquipmentService.update()`에서 기존 이미지 삭제 실패 시 예외를 잡고 계속 진행합니다. 이 전략의 장단점은?

> 💡 사용자 경험 우선 vs 고아 파일 누적, 배치 정리 작업 필요, 로깅의 중요성

### ★★★ 어려움

**Q79.** CloudFront에서 SPA의 클라이언트 사이드 라우팅을 지원하려면 어떤 설정이 필요한가요?

> 💡 403/404 에러 시 `index.html`로 커스텀 에러 응답, Origin 설정

**Q80.** EC2 t3.micro 인스턴스에서 Spring Boot + MySQL Docker 컨테이너를 운영할 때의 성능 한계와 최적화 방법은?

> 💡 메모리 제한(1GB), swap 설정, JVM 메모리 조절(-Xmx), 컨테이너 메모리 제한

**Q81.** 현재 아키텍처에서 DB가 EC2 내부 Docker에 있습니다. AWS RDS로 마이그레이션할 때의 장단점은?

> 💡 관리형 서비스의 자동 백업/패치, 비용 증가, VPC 보안 그룹 설정

**Q82.** Let's Encrypt SSL 인증서의 갱신은 어떻게 관리하나요? Nginx와의 관계는?

> 💡 Certbot 자동 갱신(crontab), Nginx 리버스 프록시의 SSL 터미네이션

---

## 8. Docker / CI/CD (10문항)

### ★ 쉬움

**Q83.** Docker의 이미지(Image)와 컨테이너(Container)의 차이는?

> 💡 이미지 = 설계도(불변), 컨테이너 = 실행 인스턴스(가변)

**Q84.** 

docker-compose.yml에서 `depends_on`과 `condition: service_healthy`의 역할은?

> 💡 서비스 시작 순서 보장, healthcheck로 실제 준비 상태 확인, 단순 `depends_on`의 한계

**Q85.** `volumes: mysql_data:/var/lib/mysql`의 역할은? Named Volume과 Bind Mount의 차이는?

> 💡 데이터 영속성, 컨테이너 삭제 후에도 데이터 유지, 성능 차이

### ★★ 보통

**Q86.** `restart: unless-stopped` 정책은 무엇이고, `always`와의 차이는?

> 💡 수동 정지 시 재시작하지 않음, 서버 재부팅 시에는 재시작

**Q87.** 

deploy-backend.yml에서 `paths` 필터가 하는 역할은?

> 💡 `backend/**`, 
> 
> docker-compose.yml 변경 시에만 배포 트리거, 불필요한 배포 방지

**Q88.** CI/CD 파이프라인에서 `.env` 파일을 EC2에 직접 생성하는 방식의 보안 문제점은?

> 💡 GitHub Secrets → SSH → .env 파일 생성, 서버에 평문 저장, AWS Secrets Manager/Parameter Store 대안

**Q89.** `docker image prune -f`를 배포 스크립트 마지막에 실행하는 이유는?

> 💡 미사용 이미지 정리, 디스크 공간 확보, t3.micro의 제한된 스토리지

### ★★★ 어려움

**Q90.** 현재 배포 방식은 **다운타임이 발생**합니다. Zero-downtime 배포를 구현하려면?

> 💡 Blue-Green 배포, Rolling Update, Docker health check + Nginx upstream, ECS/EKS

**Q91.** 현재 CI/CD에 **테스트 단계가 없습니다**. 어떤 테스트를 추가해야 하고, 파이프라인을 어떻게 구성하시겠습니까?

> 💡 단위 테스트 → 통합 테스트 → 빌드 → 배포, 테스트 실패 시 배포 중단, Test Coverage

**Q92.** Docker 멀티 스테이지 빌드(Multi-stage Build)를 적용하면 어떤 이점이 있나요?

> 💡 빌드 환경과 실행 환경 분리, 이미지 크기 감소, 보안(빌드 도구 미포함)

---

## 9. 성능 최적화 (8문항)

### ★★ 보통

**Q93.** N+1 문제란 무엇이고, 

EquipmentRequestRepository.java에서 어떻게 해결했나요?

> 💡 JPQL `JOIN FETCH`, 
> 
> findByUserId → 
> 
> findByUserIdWithItems, 쿼리 수 비교(N+1 → 1)

**Q94.** `SELECT DISTINCT r FROM EquipmentRequest r JOIN FETCH r.items i JOIN FETCH i.equipment`에서 `DISTINCT`가 필요한 이유는?

> 💡 카테시안 곱으로 인한 중복 row, JPA의 `DISTINCT`는 엔티티 수준 중복 제거

**Q95.** 

TimeTraceAop.java의 시간 측정 방식의 한계는? 프로덕션에서는 어떤 도구를 사용하나요?

> 💡 `System.out` → SLF4J 로깅, Spring Actuator + Micrometer, Prometheus + Grafana

### ★★★ 어려움

**Q96.** 

findAll() + `stream().map().collect()` 패턴은 대량 데이터에서 어떤 문제가 있나요?

> 💡 전체 로딩 → 메모리 부족 가능, 페이징 미적용, 프로젝션(DTO 직접 조회) 대안

**Q97.** `@Version`을 이용한 낙관적 잠금에서 `OptimisticLockException`이 발생하면 어떻게 재시도(Retry)를 구현할 수 있나요?

> 💡 Spring Retry `@Retryable`, 백오프 전략, 최대 재시도 횟수, 데드락과의 관계

**Q98.** JPA의 1차 캐시와 2차 캐시의 차이를 설명하세요. 현재 프로젝트에서 캐시를 적용할 수 있는 곳은?

> 💡 1차 캐시 = 영속성 컨텍스트(트랜잭션 범위), 2차 캐시 = 공유 캐시(Ehcache, Redis), 비품 목록 캐싱

---

## 10. 설계 / 아키텍처 전반 (10문항)

### ★★ 보통

**Q99.** 이 프로젝트의 레이어드 아키텍처(Controller → Service → Repository)에서 각 레이어의 책임은?

> 💡 Controller: HTTP 요청/응답 변환, Service: 비즈니스 로직, Repository: 데이터 접근

**Q100.** 

i18n.ts의 다국어 처리에서 `LanguageDetector`의 `order: ['localStorage', 'navigator']`는 어떤 의미인가요?

> 💡 언어 감지 우선순위: localStorage 저장값 → 브라우저 설정 언어, `fallbackLng: 'ko'`

**Q101.** 현재 프로젝트에서 비밀번호 유효성 검증이 어디에도 없습니다. 어디에 구현해야 하고, 프론트/백엔드 양쪽 검증이 필요한 이유는?

> 💡 프론트: UX, 백엔드: 보안, Bean Validation(`@NotBlank`, `@Size`, `@Pattern`)

### ★★★ 어려움

**Q102.** 이 프로젝트를 MSA(마이크로서비스 아키텍처)로 전환한다면 어떻게 분리하시겠습니까?

> 💡 인증 서비스, 비품 서비스, 신청 서비스, API Gateway, 서비스 간 통신(REST vs 메시지 큐)

**Q103.** 현재 동시에 두 명의 관리자가 같은 신청을 승인하면 어떤 문제가 발생하나요?

> 💡 동시성 문제, 재고 이중 감소, `@Version`으로 Equipment는 보호되지만 Request 상태 변경은 보호되지 않음

**Q104.** 이 시스템에 **알림 기능**(신청 승인/반려 시 사용자에게 알림)을 추가한다면 어떻게 설계하시겠습니까?

> 💡 WebSocket/SSE, 이메일 알림, 푸시 알림, 이벤트 기반 아키텍처(ApplicationEventPublisher)

**Q105.** 프론트엔드의 장바구니가 `localStorage`에만 존재합니다. 서버 사이드 장바구니와 비교했을 때의 장단점은?

> 💡 서버 동기화 불필요 = 간단, 기기 간 동기화 불가, 재고 실시간 반영 불가

**Q106.** 현재 

EquipmentController에서 Create/Update 시 파일 업로드(`MultipartFile`)와 메타데이터를 함께 받고 있습니다. 파일 업로드를 별도 API로 분리하는 것의 장단점은?

> 💡 Pre-signed URL 패턴, 서버 부담 감소, 대용량 파일 처리, 업로드 진행률 표시

**Q107.** 프로젝트 전체에서 개선해야 할 점을 5가지 이상 지적하고, 우선순위를 매기세요.

> 💡 ① 전역 예외 처리 ② 입력 검증(Validation) ③ 페이징 ④ 테스트 코드 ⑤ 보안 강화(token 저장) ⑥ 로깅 체계화 ⑦ API 응답 형식 통일

**Q108.** CORS 설정에서 `setAllowCredentials(true)`와 `setAllowedHeaders("*")`를 동시에 사용할 때의 주의사항은?

> 💡 `allowedOrigins("*")`와 `allowCredentials(true)`는 동시 사용 불가, 명시적 Origin 지정 필요

---

## 📌 면접 Tip

|난이도|질문 수|대상|
|---|---|---|
|★ 쉬움|약 25문항|신입/주니어|
|★★ 보통|약 45문항|주니어/미드레벨|
|★★★ 어려움|약 38문항|미드/시니어|

TIP

각 질문에 대해 단순히 "무엇인지" 뿐만 아니라 **"왜 그렇게 했는지"**, **"다른 대안은 무엇인지"**, **"트레이드오프는 무엇인지"**를 함께 준비하세요.
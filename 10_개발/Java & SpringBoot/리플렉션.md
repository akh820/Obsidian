#Java 
**리플렉션:**

- JPA는 Private을 강제로 읽어서 값을 읽고 쓸 수 있다.
- JPA가 DB에서 데이터를 가져와서 객체로 만들 때 사용하는 Java 기술
- 빈 객체 만들고 → 필드에 값 주입

```java
// JPA가 내부적으로 하는 일
User user = new User();  // ← 기본 생성자 필요!
// 리플렉션으로 필드에 값 주입
user.email = "test@test.com";
user.name = "홍길동";
```

**기본 생성자 없으면 JPA가 객체를 못 만듦!**